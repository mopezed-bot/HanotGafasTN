{
  "name": "hanout-marketplace-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root placeholder for Netlify deployment",
  "scripts": {
    "build": "cd nextjs && npm install && npm run build"
  }
}
